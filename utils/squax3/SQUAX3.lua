local c_ffi = require("ffi");
local c_api = require("utils/API/c");
local asm_api = require("utils/API/assembly");
local socket = require("socket");
local squax3={};
function squax3.asmcrash(target,attempts,bypass_count,...)
  
end;
function squax3.log4shell(file,ldap_server,bypass_count)
  
end;






return squax3;