function debug()local debug={"FPS:"..love.timer.getFPS(),};return debug;end;
local commands={
  
};
local function register_command(name,func,desc)
  
end;
local mngr={};
function mngr.command(cmd)
  local args={};
  local lcmd=cmd:lower();
 
 
end;return mngr;